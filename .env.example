# API Configuration
# Development URLs (used when NODE_ENV=development)
# These are already set in the code, no need to override unless needed

# Production URLs (required for production builds)
VITE_API_AUTH_URL=https://your-production-auth-api.com/api
VITE_API_MAIN_URL=https://your-production-api.com/api

# AWS Configuration (for CI/CD)
# Set these in GitHub Secrets
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET=your-bucket-name
# CLOUDFRONT_DISTRIBUTION_ID=your_distribution_id